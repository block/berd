# BuilderBot local development profile.
# Loaded by `bb --local-dev ...` when this file is found in the current
# directory or one of its ancestors.
# Related local services:
# - UI: http://localhost:5173/agent-manager/marketplace
# - App/CLI updates: http://localhost:8080/v1/marketplace/bb/releases/latest
# - Temporal UI: http://localhost:8233
current_profile: local-dev
profiles:
  local-dev:
    server_url: http://localhost:8080
    skills_home: .bb/local-dev/skills
